{
  "name": "react-devtool-cli",
  "version": "0.1.31",
  "description": "Agent-first CLI for inspecting React applications through a Playwright-managed browser session.",
  "type": "module",
  "files": [
    "dist/",
    "README.md"
  ],
  "bin": {
    "rdt": "dist/bin/rdt.js"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "prepack": "npm run build",
    "test": "node test/run-tests.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "playwright": "1.58.2"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "react-debug-tools": "^0.1.0",
    "react-devtools-core": "^7.0.1"
  }
}
